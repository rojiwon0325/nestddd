//import { UserEntityMapper } from '../infrastructure/user.mapper';

describe('UserEntityMapper Unit Test', () => {
  //const mapper = new UserEntityMapper();

  it('aggregate to entity 변환', () => {
    return;
  });

  it('entity to aggregate 변환', () => {
    return;
  });
});
